const 
   GEBCN = name => document.getElementsByClassName(name),
   QS = selector => document.querySelector(selector),
   GEBI = id => document.getElementById(id),
   GEBTN = name => document.getElementsByTagName(name);

const element = {
   GEBCN,
   QS,
   GEBI,
   GEBTN,
};

export default element;